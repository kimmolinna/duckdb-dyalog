status←appenderAddColumn(appender col_name)
⍝ Add column to appender active column list
⍝ appender: appender pointer
⍝ col_name: column name to activate
⍝ status: DuckDBSuccess(0) or DuckDBError(1)

status←api.duckdb_appender_add_column appender col_name

