result←stmt bindParameterIndex name;st;idx
⍝ Resolve named parameter to positional index.
⍝ result: (status index)

:If stmt=0
    result←1 0
    →0
:EndIf

(st idx)←api.duckdb_bind_parameter_index stmt 0 name
result←st idx
