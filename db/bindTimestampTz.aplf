status←stmt bindTimestampTz (idx val)
⍝ Bind TIMESTAMPTZ parameter (duckdb_timestamp micros) to prepared statement.

:If stmt=0
    status←1
    →0
:EndIf

status←api.duckdb_bind_timestamp_tz stmt idx val
