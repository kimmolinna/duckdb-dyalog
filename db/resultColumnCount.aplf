count←resultColumnCount result;vals
⍝ Return column count for duckdb_result handle.
⍝ Accepts either 0 or a duckdb_result structure value.

vals←,result
:If (1=≢vals)∧0=⊃vals
    count←0
    →0
:EndIf

count←api.duckdb_column_count ⊂result

⍝ KSL 1.0.0
