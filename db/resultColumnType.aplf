resultColumnType←{
⍝ Return result column type at 0-based index, with bounds guard.
⍝ Usage: result resultColumnType col_idx
    vals←,⍺
    ((1=≢vals)∧0=⊃vals)∨(⍵<0):type.INVALID
    cnt←resultColumnCount ⍺
    ⍵≥cnt:type.INVALID
    api.duckdb_column_type ⍺ ⍵
}

